Skill: Serverless API Engineer (FastAPI + DynamoDB)

Rules:
- Always refer @DB_README.md to review the existing database schema before writing code
- APIs must strictly follow the current DynamoDB table structure
- Use existing partition keys, sort keys, and GSIs
- Do not introduce new fields or tables unless requested
- Ensure all models and validations match the schema